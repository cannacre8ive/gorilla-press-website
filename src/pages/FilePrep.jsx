import { Button, Section } from '../components/ui';
export default function FilePrep(){const rows=[['Format','AI, EPS, or vector PDF preferred'],['Bleed','0.125 in on every side'],['Safe area','Keep important text and marks 0.125 in inside trim'],['Color','CMYK'],['Raster images','300 ppi at final size'],['Fonts','Outlined or converted to paths'],['Cut, white, gloss layers','Separate named spot-color layers using the production names supplied with the job'],['File naming','brand_product_size_version_date']];return <><section className="hero"><div className="wrap hero-copy"><p className="eyebrow">File prep</p><h1>How to send us a file that runs the first time.</h1><p className="lead">No print-ready file? Send what you have. We will tell you what needs to change.</p></div></section><Section eyebrow="Checklist" title="The fast spec"><div className="spec-table">{rows.map(([a,b])=><div key={a}><strong>{a}</strong><span>{b}</span></div>)}</div></Section><Section eyebrow="Versions" title="Name the change clearly."><p className="measure">Use separate files or clearly named artboards. Increase the version number whenever artwork changes. Avoid filenames such as <code>final-final-2</code>.</p><div className="file-example">gorilla-og-kush-2oz-v03-2026-07-28.pdf</div></Section><Section eyebrow="No source file" title="Send the best evidence you have."><p className="measure">A PNG, Canva export, screenshot, photo of an old label, or rough sketch is still useful. We will identify what can be reused and quote substantial production-artwork work separately.</p><div className="button-row"><Button to="/quote">Request a quote</Button></div></Section></>}
